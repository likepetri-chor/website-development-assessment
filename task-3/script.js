let dark_mode_enabled
const display_mode_button = document.querySelector(".display-mode-button");

const logo = document.querySelector(".logo");

/*Wait and listen for an event from the display mode button.*/
dark_mode_toggle.addEventListener("click", () => {
	/*Invert the property value, which turns it on or off.*/
	dark_mode_enabled = !dark_mode_enabled

    if (dark_mode_enabled) {

    } else {

    }
})
